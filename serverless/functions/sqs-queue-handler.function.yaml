functions:
  sqs:
    memorySize: 2048
    image:
      name: appimage
      command:
        - dist/src/app/consumer/lambda.handler
    environment:
      APP_NAME: consumer
    events:
      - sqs:
          batchSize: 1
          arn:
            Fn::GetAtt: [EmailQueue, Arn]
