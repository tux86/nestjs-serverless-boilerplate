functions:
  api:
    timeout: 30 # limits api timout to 30 seconds
    handler: dist/src/lambda-http.handler
    events:
      - httpApi:
          method: '*'
          path: /
      - httpApi:
          method: '*'
          path: /{proxy+}
